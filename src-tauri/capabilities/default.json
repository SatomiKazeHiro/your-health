{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and alert windows",
  "windows": ["main", "alert"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "store:default"
  ]
}
